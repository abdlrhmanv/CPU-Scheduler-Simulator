# =============================================================================
# TEAM CONTRACT — READ BEFORE MODIFYING
# -----------------------------------------------------------------------------
# PACKAGE MARKER ONLY — keep this file minimal (no heavy imports).
#
# Real API lives in `simulation` and algorithm submodules; UI should import
# `from engine import simulation` or specific helpers as agreed in README.
#
# =============================================================================


"""CPU scheduler engine package (pure logic, no GUI)."""
# This file marks `engine` as a Python package (namespace for scheduling modules).
# Submodules (simulation, fcfs, sjf, …) implement algorithms; UI imports `engine` only via API.
